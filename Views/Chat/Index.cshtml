@{
    // Không cần Model nữa vì sẽ load bằng AJAX
    var fullName = User.Claims.FirstOrDefault(c => c.Type == "FullName")?.Value ?? User.Identity.Name;
    var myAvatarColor = User.Claims.FirstOrDefault(c => c.Type == "AvatarColor")?.Value ?? "#1A2980";
    var myId = User.Claims.FirstOrDefault(c => c.Type == "UserId")?.Value;
}

@{
    ViewData["Title"] = "Phòng Chat";
}

<link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@500;700;800&display=swap" rel="stylesheet">
<!-- Thêm SweetAlert2 để thông báo đẹp hơn -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<style>
    /* --- MODERN VARIABLES --- */
    :root {
        --primary-color: #6c5ce7;
        --primary-gradient: linear-gradient(135deg, #a29bfe 0%, #6c5ce7 100%);
        --bg-body: #dfe6e9;
        --bg-glass: rgba(255, 255, 255, 0.9);
        --bg-sidebar: rgba(255, 255, 255, 0.95);
        --text-dark: #2d3436;
        --text-gray: #636e72;
        --border-color: rgba(0,0,0,0.08);
        --shadow-sm: 0 8px 32px 0 rgba(31, 38, 135, 0.15);
        --hover-bg: rgba(108, 92, 231, 0.1);
    }

    /* GLOBAL ANIMATIONS */
    @@keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
    @@keyframes gradientBG { 0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; } }
    
    body {
        background: linear-gradient(45deg, #ff9a9e 0%, #fad0c4 99%, #fad0c4 100%);
        background-size: 400% 400%;
        animation: gradientBG 15s ease infinite;
        font-family: 'Quicksand', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        color: var(--text-dark);
        overflow: hidden; /* Chặn scroll body chính */
        margin: 0;
    }

    /* 1. KHUNG BAO NGOÀI (CĂN CHỈNH LẠI VỊ TRÍ) */
    .chat-wrapper {
        height: 100vh;
        width: 100%;
        display: flex;
        justify-content: center;
        /* QUAN TRỌNG: Đẩy xuống 90px để né thanh Menu (Navbar) */
        padding-top: 60px;
        box-sizing: border-box;
        background: transparent;
    }

    /* 2. THẺ CHAT (CLEAN UI) */
    .chat-card {
        background: var(--bg-glass);
        backdrop-filter: blur(10px);
        width: 100%;
        max-width: 1600px; /* Rộng hơn cho màn hình lớn */
        height: 100%;
        display: flex;
        flex-direction: column;
        box-shadow: var(--shadow-sm);
        border-radius: 12px 12px 0 0;
        overflow: hidden;
    }

    /* TASKBAR */
    .taskbar {
        height: 60px;
        background: rgba(255,255,255,0.8);
        border-bottom: 1px solid var(--border-color);
        color: var(--text-dark);
        display: flex;
        align-items: center;
        padding: 0 20px;
        justify-content: space-between;
        z-index: 10;
    }
    
    .taskbar span {
        font-weight: 700;
        font-size: 1.1rem;
    }

    .chat-body {
        display: flex;
        flex: 1;
        overflow: hidden;
        background: transparent;
    }

    /* SIDEBAR (DANH SÁCH NHÓM) */
    .sidebar {
        width: 300px;
        background: var(--bg-sidebar);
        border-right: 1px solid var(--border-color);
        display: flex;
        flex-direction: column;
    }

    .sidebar-header {
        padding: 15px 20px;
        font-weight: 700;
        font-size: 1.2rem;
        color: var(--text-dark);
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .group-list {
        flex: 1;
        overflow-y: auto;
        padding: 10px;
    }

    .group-item {
        padding: 10px 15px;
        margin-bottom: 2px;
        border-radius: 12px;
        cursor: pointer;
        transition: all 0.2s;
        font-weight: 500;
        color: var(--text-dark);
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .group-item:hover,
    .group-item.active {
        background-color: var(--hover-bg);
        color: var(--primary-color);
        transform: translateX(5px);
        box-shadow: none;
    }

    /* RIGHT SIDEBAR (THÀNH VIÊN ONLINE) */
    .user-sidebar {
        width: 300px;
        background: var(--bg-sidebar);
        border-left: 1px solid var(--border-color);
        display: flex;
        flex-direction: column;
    }

    .user-list {
        flex: 1;
        overflow-y: auto;
        padding: 10px;
    }

    .user-item {
        padding: 10px;
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 0.9rem;
        color: var(--text-dark);
        cursor: pointer;
        border-radius: 12px;
        transition: background 0.2s;
    }

    .user-item:hover {
        background: var(--hover-bg);
        transform: translateX(-5px);
    }

    .section-title {
        font-size: 0.8rem;
        font-weight: 700;
        color: var(--text-gray);
        padding: 10px 15px;
        text-transform: uppercase;
    }

    .status-dot {
        width: 8px;
        height: 8px;
        background: #ccc;
        border-radius: 50%;
    }

    .status-dot.online {
        background: #00cec9;
        box-shadow: 0 0 5px #00cec9;
    }

    /* NOTIFICATION BADGE */
    .badge-count {
        background: #e74c3c;
        color: white;
        font-size: 0.7rem;
        padding: 2px 6px;
        border-radius: 10px;
        margin-left: 5px;
    }

    /* UNREAD BADGE CHO NHÓM */
    .unread-badge {
        background: #ff3b30;
        color: white;
        border-radius: 50%;
        padding: 2px 8px;
        font-size: 0.75rem;
        font-weight: bold;
        margin-left: auto; /* Đẩy sang phải */
        display: none; /* Mặc định ẩn */
    }

    /* AVATAR & STATUS */
    .avatar-container {
        position: relative;
        flex-shrink: 0;
    }
    
    .avatar {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 1rem;
        box-shadow: 0 4px 10px rgba(0,0,0,0.15);
        border: 2px solid white;
    }

    .avatar-status {
        position: absolute;
        bottom: 0;
        right: 0;
        width: 11px;
        height: 11px;
        background: #00cec9; /* Cyan status */
        border: 2px solid #fff;
        border-radius: 50%;
    }

    /* MAIN CHAT AREA */
    .main-chat-area {
        flex: 1;
        display: flex;
        flex-direction: column;
        overflow: hidden;
        background: rgba(255,255,255,0.6);
    }

    /* 3. HEADER */
    .chat-header {
        height: 60px;
        padding: 0 20px;
        border-bottom: 1px solid var(--border-color);
        background: rgba(255,255,255,0.8);
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-shrink: 0;
        z-index: 5;
        box-shadow: 0 1px 2px rgba(0,0,0,0.05);
    }

    .chat-title {
        color: var(--text-dark);
        font-weight: 700;
        font-size: 1.1rem;
        margin: 0;
    }

    /* 4. DANH SÁCH TIN NHẮN (Cuộn dọc) */
    .chat-box {
        flex: 1;
        padding: 20px;
        overflow-y: auto;
        background: transparent;
        display: flex;
        flex-direction: column;
        gap: 2px; /* Khoảng cách tin nhắn gần hơn giống Messenger */
        scroll-behavior: smooth;
    }

    /* BONG BÓNG CHAT */
    .message-item {
        display: flex;
        align-items: flex-end; /* Avatar nằm dưới cùng */
        gap: 10px;
        max-width: 75%;
        margin-bottom: 8px;
        animation: fadeIn 0.2s ease-out;
    }

    .message-content {
        padding: 12px 16px;
        border-radius: 18px;
        font-size: 0.95rem;
        line-height: 1.4;
        word-wrap: break-word;
        position: relative;
    }

    .message-content img {
        max-width: 300px;
        max-height: 300px;
        border-radius: 10px;
        padding: 0 !important;
        margin: 0 !important;
        display: block;
    }

    .message-item:has(img) .message-content {
        padding: 5px !important;
        background: transparent !important;
        box-shadow: none !important;
        border: none !important;
    }

    .message-info {
        font-size: 0.7rem;
        color: var(--text-gray);
        margin-top: 2px;
        margin-left: 5px;
        margin-right: 5px;
        display: none; /* Ẩn thời gian mặc định cho gọn */
    }
    
    .message-item:hover .message-info {
        display: block; /* Hiện khi hover */
    }

    /* Tin người khác (Trái) */
    .msg-left {
        align-self: flex-start;
    }

    .msg-left .message-content {
        background: white;
        color: var(--text-dark);
        border-bottom-left-radius: 5px;
        box-shadow: 0 2px 5px rgba(0,0,0,0.05);
    }

    /* Tin của mình (Phải) */
    .msg-right {
        align-self: flex-end;
        flex-direction: row-reverse;
    }

    .msg-right .message-content {
        background: var(--primary-gradient);
        color: white;
        border-bottom-right-radius: 5px;
        box-shadow: 0 4px 15px rgba(108, 92, 231, 0.3);
    }

    /* 5. KHUNG NHẬP LIỆU (Footer) */
    .chat-input-area {
        padding: 15px 20px;
        background: rgba(255,255,255,0.8);
        border-top: 1px solid var(--border-color);
        display: flex;
        gap: 10px;
        align-items: center;
        flex-shrink: 0;
    }

    .input-mess {
        flex: 1;
        border-radius: 25px;
        padding: 12px 20px;
        border: none;
        background: white;
        font-size: 1rem;
        outline: none;
        transition: all 0.3s;
        color: var(--text-dark);
        box-shadow: inset 0 2px 5px rgba(0,0,0,0.05);
    }

    .input-mess:focus {
        background: white;
        box-shadow: 0 0 0 2px var(--primary-color);
    }

    .btn-send {
        background: none;
        color: var(--primary-color);
        border: none;
        width: auto;
        height: auto;
        font-size: 1.4rem;
        cursor: pointer;
        transition: transform 0.2s;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        padding: 0 10px;
    }

    .btn-send:hover {
        transform: scale(1.1);
    }

    .btn-icon {
        background: none;
        border: none;
        font-size: 1.2rem;
        cursor: pointer;
        color: var(--primary-color);
        transition: background 0.2s;
        padding: 10px;
        border-radius: 50%;
    }

    .btn-icon:hover {
        background: var(--hover-bg);
        transform: scale(1.1);
    }

    /* MOBILE TOGGLE BUTTON */
    .mobile-toggle {
        display: none;
        margin-left: 10px;
    }

    /* Thanh cuộn đẹp */
    .chat-box::-webkit-scrollbar {
        width: 8px;
    }

    .chat-box::-webkit-scrollbar-thumb {
        background: #bcc0c4;
        border-radius: 4px;
    }

    .chat-box::-webkit-scrollbar-track {
        background: transparent;
    }

    /* USER ACTIONS POPUP */
    .user-actions {
        display: none;
        background: white;
        border: 1px solid #eee;
        padding: 10px;
        border-radius: 12px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        margin-top: 5px;
        animation: fadeIn 0.2s ease;
    }

    .user-item.active .user-actions {
        display: block;
    }

    .action-btn {
        display: block;
        width: 100%;
        text-align: left;
        padding: 5px;
        border: none;
        background: none;
        cursor: pointer;
        font-size: 0.85rem;
        color: var(--text-dark);
        border-radius: 5px;
    }

    .action-btn:hover {
        background: #f0f2f5;
        color: var(--primary-color);
    }

    /* RESPONSIVE */
    @@media (max-width: 900px) {
        .user-sidebar {
            position: absolute;
            right: 0;
            top: 60px;
            bottom: 0;
            background: white;
            z-index: 100;
            box-shadow: -5px 0 15px rgba(0,0,0,0.1);
            transform: translateX(100%);
            transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            width: 280px;
        }
        .user-sidebar.show { transform: translateX(0); }
        .sidebar {
            width: 80px; /* Thu nhỏ sidebar trái */
        }
        .sidebar-header span, .group-name { display: none; } /* Ẩn text sidebar trái */
        .sidebar-header { justify-content: center; padding: 10px 5px; }
        .group-item { justify-content: center; padding: 10px; }
        .mobile-toggle { display: block; }
    }
</style>

<div class="chat-wrapper">
    <div class="chat-card">
        <!-- 1.1 TASKBAR -->
        <div class="taskbar">
            <span>Xin chào, @fullName</span>
            <div style="display:flex; align-items:center; gap:15px">
                <span style="font-size: 0.8rem; opacity: 0.8;">ID: @myId</span>
                <button class="btn btn-sm btn-danger" onclick="confirmLogout()" style="font-size: 0.8rem; padding: 2px 10px;">Đăng xuất</button>
            </div>
        </div>

        <div class="chat-body">
            <!-- SIDEBAR TRÁI -->
            <div class="sidebar">
                <!-- 1. KÊNH CHUNG -->
                <div class="sidebar-header" style="padding-bottom: 5px;">
                    <span>KÊNH CHUNG</span>
                </div>
                <div style="padding: 0 10px 10px 10px;">
                     <div class="group-item active" id="globalChatBtn" onclick="joinGlobalChat()">
                        <span style="font-size:1.2rem">💬</span> 
                        <span class="group-name">Chat Cộng Đồng</span>
                     </div>
                </div>

                <!-- 2. DANH SÁCH NHÓM -->
                <div class="sidebar-header" style="border-top: 1px solid var(--border-color);">
                    <span>NHÓM CHAT</span>
                    <div>
                        <button class="btn btn-sm btn-primary rounded-circle" onclick="showCreateGroup()" title="Tạo nhóm">+</button>
                        <button class="btn btn-sm btn-secondary rounded-circle" onclick="joinByCode()" title="Nhập mã">#</button>
                    </div>
                </div>
                
                <!-- KHU VỰC LỜI MỜI VÀO NHÓM -->
                <div id="groupInvitesArea" style="display:none; background: #fff3cd; padding: 10px; border-bottom: 1px solid #eee;">
                    <div style="font-size: 0.75rem; font-weight:bold; color:#856404; margin-bottom:5px">📩 LỜI MỜI VÀO NHÓM</div>
                    <div id="groupInviteList"></div>
                </div>

                <div class="group-list" id="groupList" style="flex: 1; min-height: 100px;">
                    <!-- Danh sách nhóm sẽ load vào đây -->
                    <div class="text-center mt-3 text-muted small">Đang tải...</div>
                </div>

                <!-- 3. DANH SÁCH CHAT RIÊNG -->
                <div class="sidebar-header" style="border-top: 1px solid var(--border-color);">
                    <span>TIN NHẮN RIÊNG</span>
                </div>
                <div class="group-list" id="privateList" style="flex: 1; min-height: 100px;">
                    <!-- Danh sách chat riêng sẽ load vào đây -->
                </div>
            </div>

            <!-- KHUNG CHAT PHẢI -->
            <div class="main-chat-area">
                <div class="chat-header">
                    <button id="btnBackToGlobal" class="btn btn-sm btn-primary" style="display:none; margin-right:10px; font-weight:bold" onclick="joinGlobalChat()">🏠 Chat Chung</button>
                    <h3 class="chat-title" id="currentRoomName">💬 Chat Chung</h3>
                    <div
                        style="display: flex; align-items: center; gap: 8px; background: rgba(46, 204, 113, 0.1); padding: 5px 15px; border-radius: 20px;">
                        <span
                            style="width: 8px; height: 8px; background: #2ecc71; border-radius: 50%; box-shadow: 0 0 5px #2ecc71;"></span>
                        <span
                            style="font-weight: 800; font-size: 0.85rem; color: #2ecc71; text-transform: uppercase;">Online</span>
                    </div>
                    <div id="groupActions" style="display:none; margin-left: 10px;">
                        <button class="btn btn-sm btn-danger" onclick="leaveGroup()">Rời nhóm</button>
                        <button class="btn btn-sm btn-info" onclick="addMember()">Thêm TV</button>
                    </div>
                    <button class="btn-icon mobile-toggle" onclick="toggleUserSidebar()">👥</button>
                </div>

                <div class="chat-box" id="chatBox">
                    <!-- Messages loaded via JS -->
                </div>

                <div class="chat-input-area">
                    <button class="btn-icon" onclick="document.getElementById('fileInput').click()" title="Gửi ảnh hoặc file">📎</button>
                    <input type="file" id="fileInput" style="display:none" onchange="sendFile()">
                    <button class="btn-icon" onclick="insertEmoji('👍')">👍</button>
                    <button class="btn-icon" onclick="insertEmoji('❤️')">❤️</button>
                    <button class="btn-icon" onclick="insertEmoji('😂')">😂</button>

                    <input type="text" id="messageInput" class="input-mess" placeholder="Nhập tin nhắn..."
                        autocomplete="off">
                    <button id="sendButton" class="btn-send">➤</button>
                </div>
            </div>

            <!-- SIDEBAR PHẢI (ONLINE MEMBERS) -->
            <div class="user-sidebar" id="userSidebar">
                <div style="padding: 10px;">
                    <input type="text" id="searchUser" class="form-control form-control-sm"
                        placeholder="Tìm ID người dùng..." onkeyup="filterUsers()">
                </div>

                <div class="sidebar-header" style="font-size: 0.9rem;">
                    <span>THÀNH VIÊN ONLINE</span>
                </div>
                <ul class="user-list" id="onlineUsers"></ul>


                <div class="section-title" style="display:flex; justify-content:space-between; align-items:center">
                    <span>BẠN BÈ</span>
                    <button id="btnFriendReq" class="btn btn-sm btn-light" onclick="toggleFriendRequests(event)" title="Lời mời kết bạn">
                        🔔 <span id="friendReqCount" class="badge-count" style="display:none">0</span>
                    </button>
                </div>
                <!-- Danh sách lời mời kết bạn (Ẩn mặc định) -->
                <div class="user-list" id="requestList" style="border-bottom: 1px solid #eee; max-height: 200px; display:none; background:#fffdf0"></div>

                <div class="user-list" id="friendList" style="border-top: 1px solid #eee;">
                    <!-- Danh sách bạn bè -->
                </div>
            </div>
        </div>
    </div>

    <!-- IMAGE LIGHTBOX MODAL -->
    <div id="imageModal" style="display:none !important;position:fixed;top:0;left:0;width:100%;height:100%;background:rgba(0,0,0,0.8);z-index:9999;align-items:center;justify-content:center;">
        <div style="position:relative;max-width:90%;max-height:90%;">
            <img id="modalImage" src="" style="max-width:100%;max-height:90vh;border-radius:10px;" />
            <button onclick="closeImageModal()" style="position:absolute;top:10px;right:10px;background:#fff;border:none;border-radius:50%;width:40px;height:40px;font-size:20px;cursor:pointer;display:flex;align-items:center;justify-content:center;">✕</button>
        </div>
    </div>

    <!-- VIDEO MODAL -->
    <div id="videoModal" style="display:none !important;position:fixed;top:0;left:0;width:100%;height:100%;background:rgba(0,0,0,0.9);z-index:9999;align-items:center;justify-content:center;">
        <div style="position:relative;max-width:90%;max-height:90%;background:#000;border-radius:10px;overflow:hidden;display:flex;flex-direction:column;">
            <video id="modalVideo" controls style="width:100%;max-height:80vh;">
                <source id="modalVideoSource" src="" type="video/mp4">
            </video>
            <div style="background:#1A2980;padding:10px;display:flex;justify-content:space-between;align-items:center;gap:10px;">
                <a id="videoDownloadBtn" href="#" download style="color:#fff;text-decoration:none;padding:5px 15px;background:#007bff;border-radius:5px;flex:1;text-align:center;">📥 Tải xuống Video</a>
                <button onclick="closeVideoModal()" style="background:#fff;border:none;border-radius:50%;width:40px;height:40px;font-size:20px;cursor:pointer;display:flex;align-items:center;justify-content:center;">✕</button>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/6.0.1/signalr.js"></script>
    <!-- Truyền biến currentUser sang file JS ngoài -->
    <script>
        var currentUser = "@User.Identity.Name";
        var myAvatarColor = "@myAvatarColor";
        var currentGroup = ""; // Mặc định là chat chung hoặc rỗng
        var currentGroupId = 0;
        var currentGroupIsPrivate = false; // Track if current chat is private
        var userMap = {}; // Lưu trữ map Username -> ID
        var userColorMap = {}; // Lưu trữ map Username -> Color
        var friendIds = new Set(); // [MỚI] Lưu danh sách ID bạn bè để kiểm tra nhanh
        var lastOnlineUsers = []; // [MỚI] Lưu danh sách online gần nhất để render lại
        var userDetailsMap = {}; // [MỚI] Lưu thông tin chi tiết (FullName)

        // [FIX] Khởi tạo kết nối SignalR (Bổ sung dòng này)
        var connection = new signalR.HubConnectionBuilder().withUrl("/chatHub").build();

        // 1. NHẬN TIN NHẮN (Chung cho cả Group và Private)
        connection.on("ReceiveMessage", function (user, message, time, type) {
            if (!currentGroup) {
                appendMessage(user, message, time, type);
            }
        });

        // Nhận tin nhắn nhóm (Cần update ChatHub để dùng hàm này nếu muốn tách biệt)
        connection.on("ReceiveGroupMessage", function (user, groupName, message, time, type) {
            // 1. Nếu đang ở đúng nhóm này -> Hiện tin nhắn luôn
            if (groupName === currentGroup) {
                appendMessage(user, message, time, type);
            } 
            // 2. Nếu đang ở nhóm khác -> Hiện thông báo (Badge)
            else {
                // Tìm phần tử DOM của nhóm dựa trên data-groupname
                var groupItem = document.querySelector(`.group-item[data-groupname='${groupName}']`);
                if (groupItem) {
                    var badge = groupItem.querySelector(".unread-badge");
                    if (badge) {
                        var currentCount = parseInt(badge.innerText) || 0;
                        badge.innerText = currentCount + 1;
                        badge.style.display = "inline-block";
                        
                        // Hiệu ứng rung nhẹ hoặc đổi màu để gây chú ý
                        groupItem.style.fontWeight = "bold";
                        groupItem.style.color = "#000";
                    }
                } else {
                    // Trường hợp nhóm chưa có trong list (ví dụ chat riêng mới), reload lại list
                    loadGroups();
                }
            }
        });

        // [REAL-TIME] Lắng nghe sự kiện mời vào nhóm
        connection.on("ReceiveGroupInvitation", function() {
            loadGroupInvites(); // Tải lại danh sách lời mời ngay lập tức
            // Có thể thêm âm thanh hoặc toast thông báo ở đây
        });

        // [REAL-TIME] Lắng nghe sự kiện có nhóm mới (được thêm vào hoặc chat riêng)
        connection.on("ReceiveNewGroup", function(groupId, groupName, inviter) {
            loadGroups(); // Tải lại danh sách nhóm ngay lập tức
        });

        // [REAL-TIME] Lắng nghe lời mời kết bạn (Nếu FriendController được cài đặt để gửi)
        connection.on("ReceiveFriendRequest", function() {
            loadPendingRequests();
            // Hiển thị thông báo nhỏ
            const Toast = Swal.mixin({
                toast: true, position: 'top-end', showConfirmButton: false, timer: 3000,
                didOpen: (toast) => { toast.addEventListener('mouseenter', Swal.stopTimer); toast.addEventListener('mouseleave', Swal.resumeTimer); }
            });
            Toast.fire({ icon: 'info', title: 'Bạn có lời mời kết bạn mới' });
        });

        // 2. NHẬN DANH SÁCH ONLINE (MỚI)
        //connection.on("UserJoined", function(u, id) { loadOnlineUsers(); });
        //connection.on("UserLeft", function(u) { loadOnlineUsers(); });
        connection.on("OnlineUsersSnapshot", function (users) {
            // Khi nhận snapshot, ta cần merge với thông tin chi tiết (FullName)
            // Gọi API lấy chi tiết nếu chưa có, hoặc render tạm
            fetchAndRenderOnlineUsers(users);
        });

        function fetchAndRenderOnlineUsers(users) {
            // Gọi API lấy tất cả user để có FullName
            fetch('/Group/GetAllUsers')
                .then(r => r.json())
                .then(allUsers => {
                    // Tạo map để tra cứu nhanh
                    allUsers.forEach(u => {
                        userDetailsMap[u.username] = u.fullName;
                    });
                    renderOnlineUsers(users);
                });
        }

        function renderOnlineUsers(users) {
            lastOnlineUsers = users; // Lưu lại để dùng khi update trạng thái bạn bè
            var listHtml = "";
            users.forEach(u => {
                var name = u.username || u.Username;
                var id = u.id || u.Id;
                var fullName = userDetailsMap[name] || name; // Lấy FullName từ map, fallback là username

                // Không hiển thị chính mình
                if (name === currentUser) return;

                // Tạo màu avatar nếu chưa có
                if (!userColorMap[name]) {
                    userColorMap[name] = "#" + Math.floor(Math.random() * 16777215).toString(16);
                }

                listHtml += renderUserItem({ username: name, id: id, fullName: fullName });
            });
            document.getElementById("onlineUsers").innerHTML = listHtml;
        }

        function renderUserItem(u) {
            var name = u.username || u.Username;
            var id = u.id || u.Id;
            var fullName = u.fullName || u.FullName || name; // [FIX] Ưu tiên hiển thị FullName

            var avatar = `
            <div class="avatar-container">
                <div class="avatar" style="background:${userColorMap[name] || '#ccc'}">${name[0].toUpperCase()}</div>
                <div class="avatar-status"></div>
            </div>`;
            
            // [MỚI] Kiểm tra trạng thái bạn bè
            var isFriend = friendIds.has(id);
            var friendBadge = isFriend ? `<span style="color:#2ecc71; font-size:0.7rem; border:1px solid #2ecc71; padding:1px 4px; border-radius:4px; margin-left:5px">Đã kết bạn</span>` : '';

            // [MỚI] Render nút bấm tùy theo quan hệ
            var actionButtons = '';
            if (isFriend) {
                actionButtons = `
                    <button class="action-btn" onclick="createGroupWith(${id}, '${name}')">👥 Tạo nhóm</button>
                    <button class="action-btn" onclick="startPrivateChat('${name}')">💬 Chat riêng</button>`;
            } else {
                actionButtons = `
                    <button class="action-btn" onclick="addFriend(${id})">➕ Kết bạn</button>
                    <button class="action-btn" onclick="createGroupWith(${id}, '${name}')">👥 Tạo nhóm</button>
                    <button class="action-btn" onclick="startPrivateChat('${name}')">💬 Chat riêng</button>`;
            }

            return `
            <div class="user-item" onclick="toggleActions(this, event)">
                ${avatar}
                <div style="flex:1; margin-left:10px;">
                    <div style="font-weight:bold">${fullName} ${friendBadge}</div>
                    <div style="font-size:0.7rem">ID: ${id}</div>
                </div>
                <div class="user-actions">
                    ${actionButtons}
                </div>
            </div>
        `;
        }

        function toggleActions(el, event) {
            // Ngăn không cho sự kiện click lan ra ngoài (để không bị document bắt được và đóng ngay lập tức)
            if(event) event.stopPropagation();

            // Đóng tất cả các menu khác
            var isActive = el.classList.contains('active');
            document.querySelectorAll('.user-item').forEach(e => e.classList.remove('active'));
            
            // Nếu chưa active thì mở, nếu đang active rồi thì thôi (toggle)
            if (!isActive) {
                el.classList.add('active');
            }
        }

        // Hàm phụ trợ để hiển thị tin nhắn lên giao diện
        function appendMessage(user, message, time, type, senderFullName) {
            var chatBox = document.getElementById("chatBox");
            var isMine = (user === currentUser);
            var divItem = document.createElement("div");
            divItem.className = isMine ? "message-item msg-right" : "message-item msg-left";

            // [FIX] Lấy tên hiển thị: Ưu tiên senderFullName truyền vào -> Tra map -> Username
            var displayName = senderFullName || userDetailsMap[user] || user;
            // [FIX] Avatar lấy chữ cái đầu của Tên hiển thị (Họ tên)
            var avatarChar = displayName ? displayName.charAt(0).toUpperCase() : "?";

            var contentHtml = message;
            if (type === "Image") {
                contentHtml = `<img src="${message}" style="max-width:250px;max-height:250px;border-radius:10px;cursor:pointer;" onclick="openImageModal('${message}')" />`;
            } else if (type === "File") {
                var fileName = message.split('/').pop();
                var ext = fileName.split('.').pop().toLowerCase();
                var isVideo = ['mp4', 'avi', 'mov', 'mkv', 'webm', 'flv', 'wmv', 'm4v'].includes(ext);
                var fileIcon = getFileIcon(fileName);
                
                if (isVideo) {
                    contentHtml = `
                    <div style="background:#000;border-radius:10px;overflow:hidden;width:300px;cursor:pointer;" onclick="openVideoModal('${message}', '${ext}')">
                        <video style="width:100%;height:auto;pointer-events:none;">
                            <source src="${message}" type="video/${ext === 'mp4' ? 'mp4' : 'video/' + ext}">
                            Trình duyệt của bạn không hỗ trợ video.
                        </video>
                        <div style="padding:8px;text-align:center;background:#1A2980;color:#fff;font-size:0.9rem;">🔍 Bấm vào để xem to</div>
                    </div>`;
                } else {
                    contentHtml = `<div style="display:flex;align-items:center;gap:8px;">
                        <span style="font-size:1.5rem;">${fileIcon}</span>
                        <div style="display:flex;flex-direction:column;gap:4px;">
                            <span style="color:#1A2980;font-weight:bold;">${fileName}</span>
                            <a href="#" onclick="downloadFile('${message}', '${fileName}');return false;" style="color:#007bff;text-decoration:none;font-size:0.9rem;">📥 Tải xuống</a>
                        </div>
                    </div>`;
                }
            }

            var avatarHtml = `<div class="avatar" style="background:${isMine ? myAvatarColor : (userColorMap[user] || '#ccc')}">${avatarChar}</div>`;

            divItem.innerHTML = `
            ${avatarHtml}
            <div class="message-body">
                <div class="message-meta" style="font-size:0.75rem; color:#666; margin-bottom:2px; ${isMine ? 'text-align:right' : ''}">
                    <span style="font-weight:bold">${displayName}</span>
                </div>
                <div class="message-content">${contentHtml}</div>
                <div class="message-info">${isMine ? "Bạn" : user} • ${time}</div>
            </div>
        `;
            chatBox.appendChild(divItem);
            chatBox.scrollTop = chatBox.scrollHeight;
        }

        // Mở modal ảnh to
        function openImageModal(imageSrc) {
            var modal = document.getElementById("imageModal");
            var modalImage = document.getElementById("modalImage");
            modalImage.src = imageSrc;
            modal.style.display = "flex";
        }

        // Mở modal video to
        function openVideoModal(videoSrc, ext) {
            var modal = document.getElementById("videoModal");
            var modalVideo = document.getElementById("modalVideo");
            var modalVideoSource = document.getElementById("modalVideoSource");
            var downloadBtn = document.getElementById("videoDownloadBtn");
            var fileName = videoSrc.split('/').pop();
            
            modalVideoSource.src = videoSrc;
            modalVideoSource.type = "video/" + (ext === 'mp4' ? 'mp4' : ext);
            downloadBtn.href = videoSrc;
            downloadBtn.download = fileName;
            downloadBtn.innerText = "📥 Tải xuống " + fileName;
            
            modalVideo.load();
            modal.style.display = "flex";
        }

        // Đóng modal ảnh
        function closeImageModal() {
            var modal = document.getElementById("imageModal");
            modal.style.display = "none";
        }

        // Đóng modal video
        function closeVideoModal() {
            var modal = document.getElementById("videoModal");
            var modalVideo = document.getElementById("modalVideo");
            modalVideo.pause();
            modal.style.display = "none";
        }

        // Đóng modal khi click ngoài ảnh hoặc ESC
        window.addEventListener("DOMContentLoaded", function() {
            var imageModal = document.getElementById("imageModal");
            var videoModal = document.getElementById("videoModal");
            
            if (imageModal) {
                imageModal.addEventListener("click", function(event) {
                    if (event.target === imageModal) {
                        closeImageModal();
                    }
                });
            }

            if (videoModal) {
                videoModal.addEventListener("click", function(event) {
                    if (event.target === videoModal) {
                        closeVideoModal();
                    }
                });
            }
        });

        // Đóng modal khi nhấn ESC
        document.addEventListener("keydown", function(event) {
            if (event.key === "Escape") {
                closeImageModal();
                closeVideoModal();
            }
        });

        // Hàm tải file
        function downloadFile(fileUrl, fileName) {
            fetch(fileUrl)
                .then(response => response.blob())
                .then(blob => {
                    var url = window.URL.createObjectURL(blob);
                    var a = document.createElement('a');
                    a.href = url;
                    a.download = fileName || 'file';
                    document.body.appendChild(a);
                    a.click();
                    window.URL.revokeObjectURL(url);
                    document.body.removeChild(a);
                })
                .catch(err => console.error('Lỗi tải file:', err));
        }

        // Lấy icon file dựa trên extension
        function getFileIcon(fileName) {
            var ext = fileName.split('.').pop().toLowerCase();
            var iconMap = {
                'pdf': '📄',
                'doc': '📝', 'docx': '📝',
                'xls': '📊', 'xlsx': '📊',
                'ppt': '🎯', 'pptx': '🎯',
                'zip': '📦', 'rar': '📦', '7z': '📦',
                'mp3': '🎵', 'wav': '🎵', 'flac': '🎵',
                'mp4': '🎬', 'avi': '🎬', 'mov': '🎬', 'mkv': '🎬', 'webm': '🎬', 'flv': '🎬', 'wmv': '🎬', 'm4v': '🎬',
                'txt': '📄',
                'json': '⚙️',
                'exe': '💾',
                'apk': '📱'
            };
            return iconMap[ext] || '📎';
        }

        connection.start().then(() => {
            console.log("SignalR Connected");
        }).catch(err => console.error(err.toString()));

        document.getElementById("sendButton").addEventListener("click", function (event) {
            var input = document.getElementById("messageInput");
            if (input.value.trim() !== "") {
                if (currentGroup) {
                    connection.invoke("SendGroupMessage", currentGroup, input.value, "Text").catch(err => console.error(err.toString()));
                } else {
                    // [FIX] Truyền currentUser thay vì chuỗi rỗng ""
                    connection.invoke("SendMessage", currentUser, input.value, "Text").catch(err => console.error(err.toString()));
                }
                input.value = ""; input.focus();
            }
            event.preventDefault();
        });

        // --- FILE SENDING (IMAGE + FILE) ---
        function sendFile() {
            var file = document.getElementById('fileInput').files[0];
            if (!file) return;

<<<<<<< HEAD
            // Kiểm tra kích thước file (100MB)
            if (file.size > 100 * 1024 * 1024) {
                alert("❌ File quá lớn (tối đa 100MB)");
=======
            // Kiểm tra kích thước file (10MB)
            if (file.size > 10 * 1024 * 1024) {
                Swal.fire("Lỗi", "File quá lớn (tối đa 10MB)", "error");
>>>>>>> afac22c (Nâng cấp giao diện chat, hoàn thiện chức năng nhóm, bạn bè và lời mời tham gia nhóm)
                return;
            }

            // Tạo FormData để upload file
            var formData = new FormData();
            formData.append('file', file);

            // Hiển thị trạng thái đang upload
            var sendBtn = document.getElementById("sendButton");
            var originalText = sendBtn.innerText;
            sendBtn.disabled = true;
            sendBtn.innerText = "⏳ Đang gửi...";

            // Upload file lên server
            fetch('/Files/Upload', {
                method: 'POST',
                body: formData
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    // Detect loại file dựa trên MIME type
                    var messageType = "File";
                    if (file.type.startsWith("image/")) {
                        messageType = "Image";
                    }
                    
                    // Gửi link file qua SignalR (không gửi file nhị phân)
                    if (currentGroup) {
                        connection.invoke("SendGroupMessage", currentGroup, data.url, messageType)
                            .catch(err => console.error("Lỗi gửi file:", err));
                    } else {
                        connection.invoke("SendMessage", currentUser, data.url, messageType)
                            .catch(err => console.error("Lỗi gửi file:", err));
                    }
                } else {
                    Swal.fire("Lỗi", data.message, "error");
                }
            })
            .catch(error => {
                console.error("Lỗi upload:", error);
                Swal.fire("Lỗi", "Có lỗi xảy ra khi gửi file", "error");
            })
            .finally(() => {
                // Khôi phục trạng thái nút
                sendBtn.disabled = false;
                sendBtn.innerText = originalText;
                // Reset input file
                document.getElementById('imgInput').value = '';
            });
        }

        function insertEmoji(emoji) {
            document.getElementById("messageInput").value += emoji;
        }

        document.getElementById("messageInput").addEventListener("keyup", function (event) {
            if (event.key === "Enter") document.getElementById("sendButton").click();
        });

        window.onload = function () {
            loadGroups();
            loadFriends();
            loadPendingRequests();
            loadGroupInvites(); // Load lời mời nhóm
            
            // Tự động vào Chat Chung khi load trang
            setTimeout(() => {
                joinGlobalChat();
            }, 300);

            // [MỚI] Sự kiện click ra ngoài để đóng menu user
            document.addEventListener('click', function(e) {
                // Nếu click không trúng vào .user-item nào thì đóng hết
                if (!e.target.closest('.user-item')) {
                    document.querySelectorAll('.user-item.active').forEach(el => el.classList.remove('active'));
                }

                // [MỚI] Đóng danh sách lời mời kết bạn nếu click ra ngoài
                var reqList = document.getElementById("requestList");
                var btnReq = document.getElementById("btnFriendReq");
                if (reqList && reqList.style.display === "block" && 
                    !reqList.contains(e.target) && 
                    (!btnReq || !btnReq.contains(e.target))) {
                    reqList.style.display = "none";
                }
            });
        };

        // --- CÁC HÀM XỬ LÝ NHÓM ---

        function loadGroups() {
            fetch('/Group/GetMyGroups')
                .then(response => response.json())
                .then(data => {
                    var groupHtml = "";
                    var privateHtml = "";

                    data.forEach(g => {
<<<<<<< HEAD
                        const icon = g.isPrivate ? '👤' : '#';
                        listHtml += `<div class="group-item" id="group-${g.id}" onclick="joinRoom(${g.id}, '${g.name}', ${g.isPrivate})">${icon} ${g.name}</div>`;
=======
                        // Logic hiển thị icon và tên
                        var icon = g.isPrivate ? "🔒" : "#";
                        // Nếu là private chat, dùng tên người kia (đã xử lý ở server)
                        var displayName = g.name; 
                        var realGroupName = g.groupName; // [FIX] Lấy tên gốc để join room
                        var groupCode = g.groupCode || "";
                        
                        var itemHtml = `<div class="group-item" id="group-${g.id}" data-groupname="${realGroupName}" onclick="joinRoom(${g.id}, '${realGroupName}', '${displayName}', '${groupCode}')">
                                        <span style="font-size:1.2rem">${icon}</span>
                                        <span class="group-name">${displayName}</span>
                                        <span class="unread-badge">0</span>
                                     </div>`;
                        
                        if (g.isPrivate) {
                            privateHtml += itemHtml;
                        } else {
                            groupHtml += itemHtml;
                        }
>>>>>>> afac22c (Nâng cấp giao diện chat, hoàn thiện chức năng nhóm, bạn bè và lời mời tham gia nhóm)
                    });

                    document.getElementById("groupList").innerHTML = groupHtml || '<div class="text-center mt-2 text-muted small">Chưa có nhóm</div>';
                    document.getElementById("privateList").innerHTML = privateHtml || '<div class="text-center mt-2 text-muted small">Chưa có tin nhắn</div>';
                    
                    // Nếu đang ở trong group, highlight nó
                    if (currentGroupId) {
                        document.querySelectorAll(".group-item").forEach(el => el.classList.remove("active"));
                        var activeGroup = document.getElementById(`group-${currentGroupId}`);
                        if (activeGroup) {
                            activeGroup.classList.add("active");
                        }
                    }
                });
        }

        function joinGlobalChat() {
            console.log("Joining global chat...");
            currentGroup = "";
            currentGroupId = 0;
            currentGroupIsPrivate = false;
            document.getElementById("currentRoomName").innerText = "💬 Chat Chung";
            document.getElementById("groupActions").style.display = "none";
            document.getElementById("btnBackToGlobal").style.display = "none"; // Ẩn nút về chat chung
            
            // Highlight Chat Chung
            document.querySelectorAll(".group-item").forEach(el => el.classList.remove("active"));
            var globalBtn = document.getElementById("globalChatBtn");
            if (globalBtn) globalBtn.classList.add("active");
            
            var chatBox = document.getElementById("chatBox");
            chatBox.innerHTML = '<div class="text-center text-muted mt-3">⏳ Đang tải tin nhắn...</div>';

            // Load history chat chung
            fetch('/Chat/GetHistory')
                .then(response => {
                    console.log("Response status:", response.status);
                    if (!response.ok) {
                        throw new Error(`HTTP error! status: ${response.status}`);
                    }
                    return response.json();
                })
                .then(data => {
                    console.log("Loaded messages:", data);
                    chatBox.innerHTML = '';
                    if (!data || data.length === 0) {
                        chatBox.innerHTML = '<div class="text-center text-muted mt-3">Chưa có tin nhắn nào</div>';
                        return;
                    }
                    data.forEach(msg => {
                        console.log("Appending message:", msg);
                        appendMessage(msg.user, msg.message, msg.time, msg.type || "Text", msg.fullName); // msg.fullName từ API history chat chung (nếu có)
                    });
                })
                .catch(error => {
                    console.error('Lỗi tải lịch sử:', error);
                    chatBox.innerHTML = '<div class="text-center text-danger mt-3">❌ Lỗi tải tin nhắn: ' + error.message + '</div>';
                });

            // Join SignalR room cho Chat Chung
            connection.invoke("JoinRoom", "").catch(err => console.error("Error joining room:", err.toString()));
        }
        function joinRoom(groupId, groupName, displayName, groupCode) {

<<<<<<< HEAD
        function joinRoom(groupId, groupName, isPrivate = false) {
            currentGroup = groupName;
            currentGroupId = groupId;
            currentGroupIsPrivate = isPrivate;
            
            // Show icon based on chat type
            const icon = isPrivate ? '👤' : '🔒';
            document.getElementById("currentRoomName").innerText = icon + " " + groupName;
            
            // Hide group action buttons for private chats
            const groupActions = document.getElementById("groupActions");
            if (isPrivate) {
                groupActions.style.display = "none";
            } else {
                groupActions.style.display = "block";
            }
=======
            currentGroupId = groupId;
            // [FIX] Hiển thị tên đẹp (displayName) nhưng logic dùng tên gốc (groupName)
            var titleHtml = "🔒 " + (displayName || groupName);
            if (groupCode) {
                titleHtml += ` <span style="font-size:0.9rem; color:#666; font-weight:normal; margin-left:10px; background:#eee; padding:2px 8px; border-radius:4px">Mã: ${groupCode}</span>`;
            }
            document.getElementById("currentRoomName").innerHTML = titleHtml;
            document.getElementById("groupActions").style.display = "block";
            document.getElementById("btnBackToGlobal").style.display = "block"; // Hiện nút về chat chung
>>>>>>> afac22c (Nâng cấp giao diện chat, hoàn thiện chức năng nhóm, bạn bè và lời mời tham gia nhóm)

            // Highlight group item
            document.querySelectorAll(".group-item").forEach(el => el.classList.remove("active"));
            var activeGroup = document.getElementById(`group-${groupId}`);
            if (activeGroup) {
                activeGroup.classList.add("active");
                
                // [MỚI] Xóa badge tin nhắn chưa đọc khi click vào nhóm
                var badge = activeGroup.querySelector(".unread-badge");
                if(badge) {
                    badge.innerText = "0";
                    badge.style.display = "none";
                    activeGroup.style.fontWeight = "500"; // Reset font weight
                }
            }

            var chatBox = document.getElementById("chatBox");
            chatBox.innerHTML = '<div class="text-center text-muted mt-3">Đang tải tin nhắn...</div>';

            fetch('/Group/GetGroupHistory?groupId=' + groupId)
                .then(res => res.json())
                .then(msgs => {
                    chatBox.innerHTML = '';
                    msgs.forEach(m => appendMessage(m.sender, m.content, m.timestamp, m.type, m.senderFullName));
                });

            connection.invoke("JoinRoom", groupName).catch(err => console.error(err.toString()));
        }

        function createGroupWith(userId) {
            console.log("Creating group with user ID:", userId);
            var name = prompt("Nhập tên nhóm mới:");
            if (!name) return;
            
            fetch('/Group/CreateGroup', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ groupName: name, invitedUserId: userId })
            })
            .then(res => {
                if (!res.ok) throw new Error(`HTTP error! status: ${res.status}`);
                return res.json();
            })
            .then(data => {
                alert("✅ Tạo nhóm thành công!\nMã nhóm: " + data.code);
                loadGroups();
                
                // Tự động vào nhóm vừa tạo
                setTimeout(() => {
                    if (data.id && data.name) {
                        joinRoom(data.id, data.name);
                    }
                }, 300);
            })
            .catch(err => {
                console.error("Error creating group:", err);
                alert("❌ Lỗi tạo nhóm: " + err.message);
            });
        }

        function showCreateGroup() {
            var name = prompt("Nhập tên nhóm mới:");
            if (!name) return;
            
            var inviteId = prompt("Nhập ID người muốn mời (Optional):");
            
            fetch('/Group/CreateGroup', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ groupName: name, invitedUserId: inviteId ? parseInt(inviteId) : 0 })
            })
            .then(res => {
                if (!res.ok) throw new Error(`HTTP error! status: ${res.status}`);
                return res.json();
            })
            .then(data => {
                alert("✅ Tạo nhóm thành công!\nMã nhóm: " + data.code);
                loadGroups();
                
                // Tự động vào nhóm vừa tạo
                setTimeout(() => {
                    if (data.id && data.name) {
                        joinRoom(data.id, data.name, data.name, data.code);
                    }
                }, 300);
            })
            .catch(err => {
                console.error("Error creating group:", err);
                Swal.fire("Lỗi", "Lỗi tạo nhóm: " + err.message, "error");
            });
        }

        function joinByCode() {
            Swal.fire({
                title: 'Tham gia nhóm',
                input: 'text',
                inputLabel: 'Nhập mã nhóm',
                showCancelButton: true
            }).then((result) => {
                if (result.isConfirmed && result.value) {
                    fetch('/Group/JoinGroup?groupCode=' + result.value, { method: 'POST' })
                        .then(res => {
                            if (res.ok) { loadGroups(); Swal.fire("Thành công", "Đã tham gia nhóm!", "success"); }
                            else Swal.fire("Thất bại", "Mã nhóm không đúng!", "error");
                        });
                }
            });
        }

        function leaveGroup() {
            Swal.fire({
                title: 'Rời nhóm?',
                text: "Bạn có chắc chắn muốn rời nhóm này?",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#d33',
                confirmButtonText: 'Rời nhóm'
            }).then((result) => {
                if (result.isConfirmed) {
                    fetch('/Group/LeaveGroup?groupId=' + currentGroupId, { method: 'POST' })
                        .then(() => { loadGroups(); joinGlobalChat(); Swal.fire("Đã rời nhóm", "", "success"); });
                }
            });
        }

        function addMember() {
            Swal.fire({
                title: 'Mời thành viên',
                input: 'text',
                inputLabel: 'Nhập ID người dùng',
                showCancelButton: true
            }).then((result) => {
                if (result.isConfirmed && result.value) {
                    fetch(`/Group/InviteMember?groupId=${currentGroupId}&userId=${result.value}`, { method: 'POST' })
                        .then(async res => {
                            if (res.ok) Swal.fire("Đã gửi lời mời", "Người dùng sẽ nhận được thông báo", "success");
                            else Swal.fire("Lỗi", await res.text(), "error");
                        });
                }
            });
        }

        // --- GROUP INVITES ---
        function loadGroupInvites() {
            fetch('/Group/GetMyGroupInvitations')
                .then(r => r.json())
                .then(data => {
                    var area = document.getElementById("groupInvitesArea");
                    var list = document.getElementById("groupInviteList");
                    if (data.length > 0) {
                        area.style.display = "block";
                        var html = "";
                        data.forEach(i => {
                            html += `
                            <div style="background:white; padding:8px; border-radius:5px; margin-bottom:5px; box-shadow:0 2px 5px rgba(0,0,0,0.05)">
                                <div style="font-weight:bold; font-size:0.9rem">${i.groupName}</div>
                                <div style="font-size:0.75rem; color:#666">${i.memberCount} thành viên</div>
                                <div style="margin-top:5px; display:flex; gap:5px">
                                    <button class="btn btn-sm btn-success" style="font-size:0.7rem; padding:2px 8px" onclick="respondGroupInvite(${i.inviteId}, true)">Vào</button>
                                    <button class="btn btn-sm btn-secondary" style="font-size:0.7rem; padding:2px 8px" onclick="respondGroupInvite(${i.inviteId}, false)">Xóa</button>
                                </div>
                            </div>`;
                        });
                        list.innerHTML = html;
                    } else {
                        area.style.display = "none";
                    }
                });
        }

        function respondGroupInvite(id, accept) {
            fetch(`/Group/RespondGroupInvite?inviteId=${id}&accept=${accept}`, { method: 'POST' })
                .then(() => {
                    loadGroupInvites();
                    if(accept) {
                        loadGroups();
                        Swal.fire("Thành công", "Đã tham gia nhóm", "success");
                    }
                });
        }

        // --- FRIENDS ---
        function loadFriends() {
            fetch('/Friend/GetFriends').then(r => r.json()).then(data => {
                // [MỚI] Cập nhật Set friendIds để renderOnlineUsers dùng
                friendIds.clear();
                data.forEach(f => friendIds.add(f.Id || f.id));
                if(lastOnlineUsers.length > 0) renderOnlineUsers(lastOnlineUsers); // Re-render để cập nhật badge

                var html = "";
                data.forEach(f => {
                    // Fix: Dùng f.Username (chữ U hoa) thay vì f.username
                    var username = f.Username || f.username;  // Fallback cho cả 2 case
                    var fullName = f.FullName || f.fullName;
                    var avatarColor = f.AvatarColor || f.avatarColor;
                    var avatarChar = fullName ? fullName.charAt(0).toUpperCase() : username.charAt(0).toUpperCase();
                    
                    html += `<div class="user-item" onclick="startPrivateChat('${username}')">
                            <div class="avatar-container">
                                <div class="avatar" style="background:${avatarColor}">${avatarChar}</div>
                            </div>
                            <span style="margin-left:10px; flex:1">${fullName}</span>
                            <button class="btn btn-sm btn-outline-danger" style="padding: 2px 8px; font-size: 0.7rem;" onclick="event.stopPropagation(); removeFriend(${f.Id || f.id})" title="Xóa bạn">❌</button>
                         </div>`;
                });
                document.getElementById("friendList").innerHTML = html;
            });
        }

        function addFriend(id) {
            fetch('/Friend/AddFriend?targetId=' + id, { method: 'POST' })
                .then(res => {
                    if (res.ok) Swal.fire("Đã gửi", "Đã gửi lời mời kết bạn!", "success");
                    else Swal.fire("Lỗi", "Không thể gửi lời mời (đã là bạn hoặc đã gửi rồi)", "error");
                });
        }

        // [MỚI] Chức năng xóa bạn bè
        function removeFriend(id) {
            Swal.fire({
                title: 'Xóa bạn?',
                text: "Bạn có chắc muốn xóa người này khỏi danh sách bạn bè?",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#d33',
                confirmButtonText: 'Xóa'
            }).then((result) => {
                if (result.isConfirmed) {
                    fetch('/Friend/RemoveFriend?friendId=' + id, { method: 'POST' })
                        .then(res => {
                            if(res.ok) { Swal.fire("Đã xóa", "", "success"); loadFriends(); }
                            else Swal.fire("Lỗi", "Không thể xóa bạn (API có thể chưa được cài đặt)", "error");
                        });
                }
            });
        }

        function toggleFriendRequests(event) {
            if (event) event.stopPropagation();
            var el = document.getElementById("requestList");
            el.style.display = (el.style.display === "none") ? "block" : "none";
        }

        function loadPendingRequests() {
            fetch('/Friend/GetPendingRequests').then(r => r.json()).then(data => {
                var badge = document.getElementById("friendReqCount");
                if (data.length > 0) {
                    badge.style.display = "inline-block";
                    badge.innerText = data.length;
                    var html = "";
                    data.forEach(r => {
                        var reqName = r.requesterFullName || r.requesterName;
                        var reqChar = reqName.charAt(0).toUpperCase();
                        html += `
                        <div class="user-item" style="flex-direction:column; align-items:flex-start; cursor:default; background:white; margin:5px; border-radius:8px; border:1px solid #eee">
                            <div style="display:flex; align-items:center; width:100%">
                                <div class="avatar" style="background:${r.requesterAvatar}">${reqChar}</div>
                                <span style="margin-left:10px; font-weight:bold">${reqName}</span>
                            </div>
                            <div style="margin-top:5px; display:flex; gap:5px; width:100%">
                                <button class="btn btn-sm btn-success" style="flex:1; font-size:0.7rem" onclick="respondRequest(${r.requestId}, true)">Đồng ý</button>
                                <button class="btn btn-sm btn-secondary" style="flex:1; font-size:0.7rem" onclick="respondRequest(${r.requestId}, false)">Xóa</button>
                            </div>
                        </div>`;
                    });
                    document.getElementById("requestList").innerHTML = html;
                } else {
                    badge.style.display = "none";
                    document.getElementById("requestList").innerHTML = "<div style='padding:10px; text-align:center; font-size:0.8rem; color:#999'>Không có lời mời nào</div>";
                }
            });
        }

        function respondRequest(id, isAccept) {
            fetch(`/Friend/RespondFriendRequest?requestId=${id}&isAccept=${isAccept}`, { method: 'POST' })
                .then(() => {
                    loadPendingRequests();
                    loadFriends();
                });
        }

        // [FIX] Thêm hàm tạo nhóm với người dùng online
        function createGroupWith(userId, userName) {
            var name = prompt("Nhập tên nhóm muốn tạo với " + userName + ":");
            if (!name) return;

            fetch('/Group/CreateGroup', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ groupName: name, invitedUserId: userId })
            })
            .then(res => res.json())
            .then(data => {
                alert("✅ Tạo nhóm thành công!");
                loadGroups();
                setTimeout(() => joinRoom(data.id, data.name, data.name, data.code), 300);
            });
        }
        function startPrivateChat(username) {
            console.log("Starting private chat with:", username);
            
            if (!username) {
                Swal.fire("Lỗi", "Username không hợp lệ", "error");
                return;
            }

            fetch('/Group/CreatePrivateChat', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ targetUsername: username })
            })
            .then(res => {
                if (!res.ok) {
                    throw new Error(`HTTP error! status: ${res.status}`);
                }
                return res.json();
            })
            .then(g => {
                console.log("Private chat created:", g);
                loadGroups();
                setTimeout(() => {
<<<<<<< HEAD
                    joinRoom(g.id, g.name, true);  // Truyền true cho isPrivate
=======
                    joinRoom(g.id, g.name, username); // [FIX] Truyền username để hiển thị tên người kia
>>>>>>> afac22c (Nâng cấp giao diện chat, hoàn thiện chức năng nhóm, bạn bè và lời mời tham gia nhóm)
                }, 200);  // Delay để loadGroups hoàn thành
            })
            .catch(err => {
                console.error("Error creating private chat:", err);
                Swal.fire("Lỗi", "Lỗi tạo chat riêng: " + err.message, "error");
            });
        }

        function filterUsers() {
            var term = document.getElementById("searchUser").value.toLowerCase();
            var items = document.querySelectorAll("#userList .user-item");
            items.forEach(i => {
                if (i.innerText.toLowerCase().includes(term)) i.style.display = "flex";
                else i.style.display = "none";
            });
        }

        function confirmLogout() {
            Swal.fire({
                title: 'Đăng xuất?',
                text: "Bạn có chắc chắn muốn đăng xuất?",
                icon: 'question',
                showCancelButton: true,
                confirmButtonText: 'Đăng xuất',
                cancelButtonText: 'Hủy'
            }).then((result) => {
                if (result.isConfirmed) {
                    // Giả sử bạn dùng Cookie Auth, chuyển hướng về trang Logout
                    // Nếu chưa có Action Logout, bạn có thể dùng window.location.href = '/Account/Logout'
                    // Ở đây tôi sẽ redirect về trang chủ hoặc trang login
                    window.location.href = '/Account/Logout'; // Cần đảm bảo route này tồn tại
                }
            });
        }

        function toggleUserSidebar() {
            var sb = document.getElementById("userSidebar");
            sb.classList.toggle("show");
        }
    </script>